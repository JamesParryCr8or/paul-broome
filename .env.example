# Server only. Never use NEXT_PUBLIC for credentials.
DATABASE_URL=
GHL_PRIVATE_INTEGRATION_KEY=
GHL_LOCATION_ID=2x8A5up52ublohNgZGKU
GHL_WEBHOOK=
# JSON map: answer/metadata key -> GHL custom field ID (see README).
GHL_CUSTOM_FIELDS={}
# JSON map: diagnostic answer/metadata key -> GHL custom field ID.
GHL_DIAGNOSTIC_FIELDS={}
# Optional GHL user ID. When present, a completed diagnostic is also added as a contact note.
GHL_USER_ID=5JDCPvOuI7LhczveZlUq
# HTTPS training, calendar or next-step page shown after completion.
NEXT_STEP_URL=
CRM_RETRY_SECRET=
APP_ORIGIN=http://127.0.0.1:3000
# Until explicitly enabled, preview submissions are never stored or sent.
LEAD_CAPTURE_ENABLED=false
